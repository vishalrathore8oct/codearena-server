openapi: 3.0.0
info:
  title: CodeArena API
  version: 1.0.0
  description: API documentation for CodeArena Backend

servers:
  - url: http://localhost:4000

paths:
  /:
    get:
      summary: Home route
      responses:
        "200":
          description: Success response

  /error:
    get:
      summary: Test error route
      responses:
        "500":
          description: Error response
